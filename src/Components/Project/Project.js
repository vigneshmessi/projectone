/*import React from 'react';
import { useSearchParams } from 'react-router-dom';

function Profile() {
  const [searchParams] = useSearchParams();
  const name = searchParams.get('Vignesh');

  return (
    <div>
      <h1>Welcome, {name}!</h1>
    </div>
  );
}

export default Profile;*/

